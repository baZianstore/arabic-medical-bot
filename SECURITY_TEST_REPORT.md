# تقرير اختبار الأمان

> `SECURITY-GATE: healthcare-app-security-guardrails loaded — phase=testing — status=ENFORCED`

تاريخ الفحص: 2026-09-16. استخدمت الاختبارات بيانات اصطناعية فقط، ولم تتصل بمشروع Supabase أو بوت Telegram حقيقي.

| الضابط | النتيجة | الدليل |
|---|---|---|
| لا PHI | ناجح ضمن نطاق الكود | لا توجد حقول مريض أو تقرير أو وصفة؛ المحتوى عام، ومعرّف Telegram يستخدم للتوصيل فقط. |
| جلسة المتصفح | ناجح | Cookie تحمل `HttpOnly` و`SameSite=Lax`، و`Secure` مفروضة في الإنتاج. لا Web Storage. |
| RBAC | ناجح | المسارات الإدارية ترفض المجهول، والدور الوحيد `admin`، وSupabase يرفض `anon/authenticated`. |
| CSRF | ناجح | اختبار POST للدخول دون token أعاد 400؛ الحذف والخروج محميان أيضًا. |
| XSS | ناجح لوظيفة التنسيق | اختبار تنسيق المرض أكد escape لنص HTML، وJinja autoescape مفعّل للقوالب. |
| الأسرار | ناجح | فحص المصدر لم يجد مفاتيح ثابتة؛ `.env` مستبعد من Git. |
| الرؤوس الأمنية | ناجح | CSP وReferrer-Policy وX-Frame-Options وnosniff وPermissions-Policy. |
| webhook والمجدول | ناجح تصميميًا | أسرار مستقلة ومقارنة ثابتة الزمن، والرفض الافتراضي عند غياب التهيئة. |
| الاعتماديات | ناجح | إصدارات مثبتة في `requirements.txt`. |
| الاختبارات | ناجح | 19 اختبارًا مرّ بنجاح، مع تجميع Python وفحص YAML والقوالب. |

## خطر متبقٍ

لا يمكن اختبار رحلة Telegram وSupabase الحقيقية دون مفاتيح المستخدم. الخطة المجانية في Render قد تنام، كما قد تتأخر GitHub Actions؛ لذلك لا يوجد ضمان نشر في الدقيقة 08:00. هذه ضوابط هندسية وليست شهادة امتثال قانوني.
