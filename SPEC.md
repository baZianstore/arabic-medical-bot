# مواصفة: بوت تيليغرام عربي للتعليم في الطب الباطني

## الهدف

بناء نظام Python 3.10+ يقدّم محتوى طبيًا تعليميًا عامًا بالعربية عبر تيليغرام، مع لوحة إدارة عربية متجاوبة على iPad. يدعم البوت `/start` و`/help` و`/today` و`/search` و`/quiz` و`/systems` و`/stats`، وينشر مرض اليوم عند الساعة 08:00 وفق `Asia/Jerusalem`.

المحتوى تعليمي ولا يستقبل أعراضًا أو تقارير أو ملفات مرضى، ولا يشخّص أو يصف علاجًا فرديًا.

## المكدس التقني

Python 3.10+، python-telegram-bot 22.8، Flask 3.1.3، Flask-WTF 1.3.0، Supabase Python 2.31.0، APScheduler 3.11.3، Uvicorn، Bootstrap RTL 5.3.8 محلي داخل المشروع، وpytest.

## أوامر التشغيل

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python seed_data.py
python main.py
pytest -q
```

## بنية المشروع

```text
main.py                 نقطة التشغيل المحلية والسحابية
bot.py                  منطق تيليغرام والجدولة
database.py             طبقة Supabase
web.py                  تطبيق Flask والمسارات
forms.py                نماذج WTForms والتحقق
config.py               تحميل الإعدادات والتحقق منها
schema.sql               مخطط PostgreSQL وRLS
seed_data.py             إدخال خمسة أمراض و15 سؤالًا
requirements.txt         إصدارات الاعتماديات المثبتة
render.yaml              إعداد Render
.github/workflows/       منبّه مجاني للنشر اليومي
templates/               واجهة عربية RTL
static/                  Bootstrap محلي وتنسيق الواجهة
tests/                   اختبارات وحدات وتكامل مع بيانات اصطناعية
```

## أسلوب الكود

تكون أسماء الدوال والمتغيرات بالإنجليزية المعيارية، وتكون التعليقات والشروح بالعربية. تتعامل طبقة قاعدة البيانات وحدها مع Supabase. تُعرض للمستخدم رسائل عربية عامة عند الخطأ دون تفاصيل داخلية أو أسرار.

```python
def get_disease(self, disease_id: str) -> dict | None:
    """إرجاع مرض واحد، أو None إذا لم يكن موجودًا."""
    response = self.client.table("diseases").select("*").eq("id", disease_id).limit(1).execute()
    return response.data[0] if response.data else None
```

## استراتيجية الاختبار

تُستخدم قواعد بيانات وهمية وبيانات اصطناعية فقط. تغطي الاختبارات الإعدادات، تنسيق رسائل البوت، البحث، الاختبار متعدد الخيارات، مصادقة لوحة الإدارة، رفض المجهول، CSRF، CRUD، حماية endpoint المجدول، والرؤوس الأمنية. لا تُستخدم مفاتيح حقيقية ولا اتصالات إنتاجية.

## الحدود

- **دائمًا:** التحقق من المدخلات، حماية POST بـCSRF، إبقاء مفاتيح Telegram وSupabase في الخادم، استخدام Cookie جلسة `HttpOnly` و`SameSite=Lax` و`Secure` في الإنتاج، ومنع الوصول افتراضيًا.
- **عند النشر:** يجب تغيير كلمة المرور الافتراضية، وإنشاء أسرار طويلة، واستخدام HTTPS ومفتاح Supabase سري خادمي.
- **ممنوع:** بيانات مرضى أو أعراض مرتبطة بهوية، أسرار في Git، جلسات في Web Storage، عرض مفتاح Supabase السري للمتصفح، أو فتح جداول Supabase للدورين `anon` و`authenticated`.

## معايير النجاح

1. تعمل جميع أوامر البوت بالعربية، ويعرض `/quiz` أربعة أزرار ويشرح الإجابة.
2. تخزّن Supabase خمسة أمراض و15 سؤالًا، مع مشتركين ونشر يومي idempotent.
3. تسمح لوحة الإدارة للمشرف وحده بعرض وإضافة وتعديل وحذف الأمراض والأسئلة.
4. تستخدم الواجهة `lang="ar"` و`dir="rtl"` وتنجح على عرض 768 بكسل دون تمرير أفقي غير ضروري.
5. يعمل التطبيق محليًا بالاقتراع، وسحابيًا عبر webhook، وتوجد آلية مجانية لإيقاظ الخدمة عند 08:00.
6. تمر الاختبارات الآلية وفحص الأسرار وتجميع Python دون أخطاء.

## الافتراضات المعتمدة

اختار المستخدم النسخة الموسعة: Supabase وخمسة أمراض وCRUD كامل. سيُستخدم مفتاح Supabase السري على الخادم؛ يبقى `SUPABASE_ANON_KEY` في القالب للتوافق فقط ولا يُوصى به لهذا التطبيق الإداري. Render المجاني مناسب للتجربة لكنه لا يضمن خدمة دائمة بسبب السكون بعد الخمول.
