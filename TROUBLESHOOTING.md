# دليل استكشاف الأخطاء

ابدأ دائمًا من أبسط فحص: افتح `https://رابط-خدمتك/health`. إذا ظهرت `{"status":"ok"}` فالخدمة تعمل، والمشكلة غالبًا في Telegram أو Supabase أو متغيرات البيئة.

| المشكلة | السبب المحتمل | الحل العملي |
|---|---|---|
| تظهر رسالة `متغيرات البيئة الناقصة` | قيمة مطلوبة غير موجودة | افتح Render → Environment وأضف الاسم المذكور حرفيًا، ثم أعد النشر. |
| ترفض الخدمة كلمة `admin123` عند النشر | حماية مقصودة في الإنتاج | ضع `ADMIN_PASSWORD` قوية وفريدة من 12 محرفًا على الأقل. |
| البوت لا يرد | رمز خاطئ، webhook خاطئ، أو الخدمة نائمة | افتح `/health` لإيقاظ الخدمة، ثم راجع `BOT_TOKEN` و`WEBHOOK_URL` وسجل Render. |
| خطأ `Conflict: terminated by other getUpdates request` | توجد نسختان تعملان بالـpolling | أوقف إحدى النسختين. لا تشغل البوت محليًا مرتين. |
| `getUpdates` لا يعمل | ما زال webhook السحابي مفعّلًا | أوقف الخدمة السحابية واحذف webhook مؤقتًا قبل polling المحلي. |
| خطأ Supabase `401` | URL أو المفتاح غير صحيح | انسخ `SUPABASE_URL` و`SUPABASE_SECRET_KEY` من مشروعك نفسه، دون فراغات. |
| خطأ Supabase `42501 permission denied` | لم ينفذ `schema.sql` كاملًا أو استخدم مفتاحًا عامًا | نفذ المخطط كاملًا واستخدم المفتاح السري الخادمي. |
| الجداول موجودة لكن الطلبات فارغة | لم تشغل بيانات البداية | نفذ `python seed_data.py`. |
| تتكرر البيانات | سكربت قديم أو إضافة يدوية | السكربت الحالي يحدّث الأمراض بالاسم الإنجليزي ويستبدل أسئلتها. احذف التكرار اليدوي مرة واحدة. |
| لوحة الإدارة تعيدك إلى الدخول | Cookie محجوبة أو انتهت بعد 30 دقيقة | فعّل Cookies، افتح رابط HTTPS نفسه، وسجل الدخول مجددًا. |
| رسالة انتهاء صلاحية النموذج | انتهى CSRF أو الصفحة قديمة | أعد تحميل الصفحة ثم أعد الإرسال. لا تفتح نموذجًا قديمًا ساعات. |
| زر الحذف لا يعمل | JavaScript محجوب أو CSRF منتهي | حدّث الصفحة. الحذف نفسه POST محمي ولا يُسمح عبر رابط GET. |
| النشر اليومي لم يصل في 08:00 | Render كان نائمًا أو GitHub Actions تأخر | تحقق من Actions ومن سري `APP_URL` و`SCHEDULER_SECRET`. الخطة المجانية لا تضمن الدقيقة المحددة. |
| وصل النشر مرتين | جدول `daily_publications` مفقود أو حُذف سجل اليوم | أعد تنفيذ `schema.sql` وتجنب حذف سجل اليوم قبل انتهاء اليوم. |
| Render يعرض فشل health check | التطبيق لم يبدأ أو متغير إنتاج ناقص | افتح Logs وابحث عن `خطأ في الإعدادات`؛ صحح الاسم المذكور دون نشر قيمة السر في أي مكان. |
| الصور لا تظهر | `image_url` غير صالح أو المضيف يمنع hotlink | استخدم رابط HTTPS مباشرًا من مصدر مسموح، أو اترك الحقل فارغًا. البوت الحالي لا يحتاج صورة. |
| العربية غير مرتبة | صفحة قديمة في cache | أعد التحميل القسري. القوالب تستخدم `lang="ar"` و`dir="rtl"` وBootstrap RTL محليًا. |

## فحوص آمنة من الطرفية

نفذ هذه الأوامر داخل مجلد المشروع:

```bash
source .venv/bin/activate
pytest -q
python -m compileall -q .
python -c "from config import Settings; print(Settings.from_env().bot_mode)"
```

الأمر الأخير يطبع وضع التشغيل فقط، ولا يطبع مفاتيحك. لا تستخدم `printenv` أو `cat .env` عند مشاركة الشاشة أو سجل الدعم.

## استعادة الرمز أو المفتاح عند التسرب

إذا ظهر `BOT_TOKEN` علنًا، استخدم BotFather لإلغائه وإنشاء رمز جديد، ثم حدّث Render. إذا ظهر مفتاح Supabase السري، أنشئ مفتاحًا سريًا جديدًا، حدّث الخدمة، تحقق من عملها، ثم احذف المفتاح المتسرب. لا تضع المفتاح الجديد في GitHub أو رسالة بريد.

## متى تحتاج مساعدة خارجية؟

إذا كان خطأ Render متعلقًا بالفوترة أو الحساب، استخدم دعم Render. وإذا كان الخطأ في حساب Manus أو الرصيد أو الاشتراك، استخدم [مركز مساعدة Manus](https://help.manus.im). أما أخطاء هذا المشروع فابدأ بسجل Render واختبار `/health` ورسالة الخطأ العامة.

## المراجع

[1]: https://supabase.com/docs/guides/api/api-keys "Supabase API keys and rotation"
[2]: https://core.telegram.org/bots/api "Telegram Bot API"
[3]: https://render.com/docs/troubleshooting-deploys "Render deployment troubleshooting"
